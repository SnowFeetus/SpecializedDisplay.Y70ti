# Design pointer

The full extraction design (all three SpecializedDisplay repos + the hwmonitor refactor) lives in
the sibling repo: `..\..\SpecializedDisplay.NET\docs\DESIGN.md`.

Sections that govern THIS repo:
- §1 Repo scaffolding (ProjectReference to both sibling libs)
- §4 SpecializedDisplay.Y70ti — Y70tiDisplay/Y70tiTouch API, constants, tests, device-notes.md content
- §6 Risk register item #1 (selector last-match-wins semantics)

Source material: Y70ti sections of `..\..\Y70ti_Exclusive_HWMonitor\docs\{display-findings,touch-findings}.md`.
